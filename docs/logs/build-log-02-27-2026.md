
02-07-2026

objectives




    1.	Finish lifecycle enforcement fully
	2.	Build automation runner
	3.	Verify contract orchestration end-to-end
	4.	Build editor logic layer
	5.	Build workspace logic layer
	6.	Lock engine
	7.	Only then begin API

That is sequential.
That is stable.
That is how you prevent drift.

------------------------------------------------------


Spec record for your documentation (clean + concrete)

File: backend/engine/contracts/services/activation_service.py
Status: ⚠️ Partially correct, required enforcement fix
Why: Original version attempted to persist ServiceObligation into ContractObligation schema (would crash). Also did not run lifecycle engine prior to persisting state (would persist incorrect initial states when start_date is in past).
Fix applied: Activation now persists only obligations compatible with persistence schema (Payment obligations) and derives state through process_obligation_lifecycle() before saving.
Remaining gap (explicit): Service obligations require a dedicated persistence model/table (or schema extension) before they can be stored and enforced end-to-end.



backend.engine.contracts.obligations.state module does not exist, but payment_service.py imports from it → broken enforcement path.
Fix: payment_service.py now uses process_obligation_lifecycle() as the single lifecycle truth after applying payment facts; removes duplicate evaluator/escalation imports.
Result: Payment updates cannot bypass lifecycle enforcement.



