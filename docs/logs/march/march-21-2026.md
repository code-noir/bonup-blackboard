#docs/logs/march/march-21-2026.md

Main theme of the day

You expanded Blackboard from:
	•	contract-centered workflow access

into:
	•	obligation-centered read surfaces
	•	obligation-native shortcuts
	•	stronger domain separation between contracts and obligations

That was the core of the day.

⸻

What got built yesterday

1. Obligation detail endpoint

You added an obligation-level detail surface.

Endpoint

GET /api/obligations/<obligation_type>/<obligation_id>/

What it returns
	•	obligation identity
	•	type
	•	contract id
	•	state
	•	due date
	•	key obligation fields
	•	execution summary
	•	approval summary
	•	adjustment summary
	•	promotion summary

Why it matters

This became the base entry point for the obligation domain.

⸻

2. Obligation timeline endpoint

You added a chronological obligation history endpoint.

Endpoint

GET /api/obligations/<obligation_type>/<obligation_id>/timeline/

What it returns

A timeline combining:
	•	execution sessions
	•	execution events
	•	approval requests
	•	value adjustments
	•	promotions

Why it matters

This gives a real history trail for what happened under one obligation.

⸻

3. Obligation list endpoint

You added an obligation list surface with filtering.

Endpoint

GET /api/obligations/

Supported filters
	•	type
	•	state
	•	role
	•	user_id

Why it matters

This created the first obligation-domain collection endpoint, not just single-obligation views.

⸻

4. Obligation next-actions endpoint

You added an endpoint to tell the system what should happen next for an obligation.

Endpoint

GET /api/obligations/<obligation_type>/<obligation_id>/next-actions/

What it returns

Action-oriented signals like:
	•	payment required
	•	review pending approvals
	•	review value adjustments
	•	review promoted side obligations
	•	no action

Why it matters

This is one of the first obligation endpoints that is directly useful for future admin UI, dashboard cards, and AI guidance.

⸻

5. Obligation dashboard summary

You completed the first lightweight dashboard summary for the obligation area.

Endpoint

GET /api/obligations/dashboard-summary/

What it returns
	•	applied filters
	•	counts
	•	breakdown by state
	•	attention summary

Key design boundary

This was deliberately kept:
	•	lightweight
	•	obligation-focused
	•	not a second contract management summary

Why it matters

This gives the obligation area its own top-of-page summary surface.

⸻

6. Obligation proof shortcut

You added an obligation-domain proof route.

Endpoint

GET /api/obligations/<obligation_type>/<obligation_id>/proof/

What it does

It acts as a thin shortcut into the existing proof behavior.

What was verified

You tested parity between:
	•	contract-side proof
	•	obligation-side proof

for both:
	•	service obligations
	•	payment obligations

The payloads matched, so the shortcut was confirmed to be trustworthy.

Why it matters

This means proof can now be accessed directly from the obligation domain without rethinking proof logic.

⸻

7. Obligation execution sessions shortcut

You added a read shortcut for execution sessions from the obligation side.

Endpoint

GET /api/obligations/<obligation_type>/<obligation_id>/execution-sessions/

What was verified
	•	service side returned real sessions
	•	payment side returned clean empty output
	•	parity with contract-side execution session list was confirmed

Why it matters

This keeps execution accessible from the obligation context.

⸻

8. Obligation execution events shortcut

You added a read shortcut for execution events from the obligation side.

Endpoint

GET /api/obligations/<obligation_type>/<obligation_id>/execution-events/

What happened

There was a brief 404 because urls.py had not been saved.
Once saved, the route worked.

Why it matters

Now the actual execution events under an obligation can be pulled directly from the obligation domain.

⸻

9. Obligation approval requests shortcut

You added approval request access from the obligation side.

Endpoint

GET /api/obligations/<obligation_type>/<obligation_id>/approval-requests/

What was verified
	•	service obligation returned real approval request records
	•	payment obligation returned []

Why it matters

Approvals are now directly available under obligation context, which is important for UI and workflow review.

⸻

10. Obligation value adjustments shortcut

You added obligation-side access to value adjustments.

Endpoint

GET /api/obligations/<obligation_type>/<obligation_id>/value-adjustments/

What was verified
	•	service side returned real adjustments
	•	payment side returned clean empty output for the tested obligation

Why it matters

Adjustments are now directly visible from the obligation side, not only through proof or contract management.

⸻

11. Obligation promotions shortcut

You added direct access to promotions from the obligation side.

Endpoint

GET /api/obligations/<obligation_type>/<obligation_id>/promotions/

What was verified
	•	service side returned real promotion records
	•	payment side returned []

Why it matters

Promotion linkage is now visible directly from the obligation.

⸻

12. Obligation promoted-side-obligations shortcut

You added direct access to the promoted child obligations from a parent service obligation.

Endpoint

GET /api/obligations/<obligation_type>/<obligation_id>/promoted-side-obligations/

What was verified
	•	service side returned real child obligations
	•	payment side returned []

Why it matters

This makes side-obligation relationships visible in a clean obligation-native way.

⸻

Testing work completed yesterday

A lot of yesterday’s work was not just code. You also tightened the testing discipline.

Testing patterns used

You verified:
	•	smoke behavior
	•	filter behavior
	•	empty-state behavior
	•	parity between contract-side and obligation-side shortcuts

Important tests completed
	•	obligation proof shortcut parity test
	•	execution sessions shortcut parity test
	•	dashboard summary shape and filter tests
	•	empty response tests for payment-side endpoints
	•	service-side real data confirmation for proof, approvals, promotions, adjustments

⸻

Architecture / workflow decisions made yesterday

1. Obligation domain is additive, not a replacement for contracts

You confirmed that obligation endpoints are being added alongside contracts, not by moving everything out of contracts immediately.

2. Thin shortcut pattern is valid

You confirmed that a lot of these obligation endpoints are intentionally:
	•	thin
	•	read-only
	•	obligation-rooted access paths
	•	reusing existing truth

3. Dashboard summary should stay lightweight

You explicitly kept obligation dashboard summary from turning into:
	•	contract management summary
	•	a giant operations report

4. Proof and PBVD boundary stayed clear

You kept:
	•	Blackboard proof = contract-bound proof
	•	PBVD = broader BurnUp proof/documentation layer

That was an important conceptual checkpoint.

⸻

Process / workflow improvements yesterday

1. You adjusted how edits should be made

You decided big files should preferably be updated by:
	•	class
	•	section
	•	targeted addition

instead of whole-file replacement every time.

That was a good operational change.

2. You tightened trust standards for code generation

You stress-tested the other GPT with architectural questions and testing rules.

You also clarified that code trust now depends on:
	•	tracking current build state
	•	respecting boundaries
	•	not repeating already-completed work

3. You locked known working IDs for testing

You established stable IDs for reuse in curl tests:
	•	contract id
	•	service obligation id
	•	payment obligation id
	•	execution session id
	•	execution event id
	•	user id

That reduces friction going forward.

⸻

Endpoint count movement

By the end of yesterday, the obligation domain had grown into a real API surface.

Obligation endpoints built

You ended up with about 12 obligation endpoints, including:
	•	list
	•	dashboard summary
	•	detail
	•	timeline
	•	next-actions
	•	proof
	•	execution sessions
	•	execution events
	•	approval requests
	•	value adjustments
	•	promotions
	•	promoted side obligations

Whole confirmed total

You were around 31 confirmed endpoints total by the end of that sprint.

⸻

What yesterday really accomplished

The biggest real accomplishment was this:

Obligations stopped being just an internal concept and became a first-class working API domain.

That is what happened.

Before yesterday, obligation behavior existed, but the domain surface was much thinner.

After yesterday, the obligation side now has:
	•	overview
	•	detail
	•	timeline
	•	next-actions
	•	proof
	•	execution visibility
	•	approval visibility
	•	adjustment visibility
	•	promotion visibility

That is a big jump.

⸻

Best one-line summary for your records

Yesterday was the obligation-domain expansion day: Blackboard gained a full set of obligation-native read surfaces and thin shortcuts, making obligations a real first-class API domain rather than just a contract sub-object.

If you want, I can turn this into a tighter worklog / changelog format with bullets and timestamps style for your notes.





