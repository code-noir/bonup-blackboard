work pushed the system further by adding missing write actions and closing more lifecycle gaps.

You completed:

1. Obligation approval request create

You added and tested:
	•	POST /api/obligations/<obligation_type>/<obligation_id>/approval-requests/

What happened:
	•	reused the approval service
	•	made the obligation-side approval request route support both GET and POST
	•	tested with a real execution event id
	•	confirmed a new pending approval request was created successfully

Result:
	•	obligation approval flow is now complete on the obligation side:
	•	list
	•	create
	•	approve
	•	reject

⸻

2. Contract-side value adjustment create

You added and tested:
	•	POST /api/contracts/obligations/<obligation_type>/<obligation_id>/value-adjustments/

What happened:
	•	confirmed there was no prior API entrypoint for value-adjustment writes
	•	used the existing ValueAdjustmentService
	•	intentionally limited V1 write behavior to manual additional_charge
	•	created serializer support
	•	created the contract-side value adjustment view
	•	wired the route in contracts/urls.py
	•	tested successfully with a real execution event id

Result:
	•	contract-side value adjustment creation now works
	•	persisted additional charges are linked to the right event and service obligation

⸻

3. Obligation-side value adjustment create

You added and tested:
	•	POST /api/obligations/<obligation_type>/<obligation_id>/value-adjustments/

What happened:
	•	mirrored the contract-side additional-charge flow into the obligation API
	•	fixed route wiring issues where POST was still hitting the old GET-only class
	•	replaced the old read-only value adjustment class with a combined list/create class
	•	tested successfully after correcting the URL binding

Result:
	•	obligation-side value adjustment list/create now works
	•	both contract and obligation contexts can now create manual additional charges

⸻

4. Obligation service resolve

You added and tested:
	•	POST /api/obligations/<obligation_type>/<obligation_id>/resolve/

V1 behavior:
	•	supports service obligations
	•	sets:
	•	state = "resolved"
	•	completed_at = now()
	•	rejects payment resolve for now

What happened:
	•	fixed import/class-name mismatch in obligations/urls.py
	•	tested successfully against a real service obligation

Result:
	•	service obligations can now be resolved directly from obligation context

⸻

5. Contract-side service resolve

You added and tested:
	•	POST /api/contracts/obligations/<obligation_type>/<obligation_id>/resolve/

What happened:
	•	confirmed no contract-side resolve action existed
	•	created resolve_views.py
	•	wired the contract-side resolve route
	•	fixed the import/class-name mismatch in contracts/urls.py
	•	tested successfully with the same real service obligation

Result:
	•	service obligations can now be resolved from contract context too

⸻

Product / architecture thinking you clarified today

You also clarified a few important product concepts:
	•	Either party can initiate a deal
	•	initiator/counterparty is universal
	•	this is not an engine issue as much as a workflow/template/API issue
	•	Templates are a major feature lane
	•	templates belong to contract creation, before signature
	•	lifecycle management is a separate post-signature phase
	•	Two pre-signature workflow concepts emerged
	•	direct draft
	•	draft and selection
	•	Template taxonomy direction
	•	category
	•	subcategory
	•	template
	•	guided setup
	•	generated contract + obligations

Those were design conclusions, not just random discussion.

Net practical outcome

By the end of today, your obligation action lane got significantly stronger.

Working action-oriented endpoints confirmed today
	•	create approval request from obligation context
	•	create value adjustment from contract context
	•	create value adjustment from obligation context
	•	resolve service obligation from obligation context
	•	resolve service obligation from contract context
