# Next Task

Outstanding work, in roughly the order Project Claude has recommended:

1. Continue May 18 app workflow verification after auth/signup:
   - login/logout
   - password reset/change
   - BON ID
   - admin/operator console
   - desktop Blackboard workflow audit

2. Paste project instructions into Claude.ai project settings. Template
   was drafted by ChatGPT earlier in the project. Not yet pasted into
   the web interface. Separate from CLAUDE.md, which is for Claude Code
   in the terminal.

3. Branch cleanup. `restore-before-break` is truth and is pushed. Run
   `git branch -a` when ready to decide what to delete and whether to
   consolidate with main.

4. Vision update. One or two features have been built that are not yet
   reflected in the founder's vision document. Reconcile.

5. H-series backend hardening items:
   - file size limit
   - MIME validation
   - lifecycle gate on contract-scoped payment create

6. Document remaining secondary backend domains: ai, notifications,
   engine, search, negotiation_prep, infrastructure, admin.

7. Frontend audit.
